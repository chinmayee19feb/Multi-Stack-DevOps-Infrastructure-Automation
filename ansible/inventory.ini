# Frontend instances (public facing - users access these)
[frontend]
voting-frontend

# Backend instances (private - run Redis and Worker services)
[backend]
voting-backend

# Database instances (private - PostgreSQL database)
[db]
voting-db
